services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-interview-code:local
    container_name: my-interview-code
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
    restart: unless-stopped